{
  "license": "UNLICENSED",
  "scripts": {
    "serve": "hugo server",
    "fix:lint:md": "find . -type d -name node_modules -prune -o -type f -name '*.md' -exec npx markdownlint --config .markdownlintrc.json --fix {} \\;",
    "check:lint:md": "find . -type d -name node_modules -prune -o -type f -name '*.md' -exec npx markdownlint --config .markdownlintrc.json {} \\;"
  },
  "dependencies": {
    "markdownlint": "^0.20.1",
    "markdownlint-cli": "^0.22.0"
  }
}
